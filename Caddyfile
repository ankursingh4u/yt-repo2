# Coolify's Traefik proxy terminates TLS for the domain and forwards here on :80.
# Caddy only enforces HTTP Basic Auth, then proxies to the renderer internally.
{
	auto_https off
	admin off
}

:80 {
	basic_auth {
		{$SVM_USER} {$SVM_PASS_HASH}
	}
	reverse_proxy short-video-maker:3123
}
